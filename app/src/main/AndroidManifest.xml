<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xixi" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".ui.main.MainActivity" -->
        <!-- android:label="@string/title_activity_main" > -->
        <!-- </activity> -->
        <activity
            android:name=".ui.magpie.MagpieActivity"
            android:label="@string/title_activity_magpie" >
        </activity>
        <activity
            android:name=".ui.magpie.NewMagpieActivity"
            android:label="@string/title_activity_new_magpie" >
        </activity>
        <activity
            android:name=".ui.me.ProfileActivity"
            android:label="@string/title_activity_profile" >
        </activity>
        <activity
            android:name=".ui.start.IntroActivity"
            android:label="@string/title_activity_intro" >
        </activity>
        <activity
            android:name=".ui.start.LoginActivity"
            android:label="@string/title_activity_login" >
        </activity>
        <activity
            android:name=".ui.start.RegisterActivity"
            android:label="@string/title_activity_register" >
        </activity>
        <activity
            android:name=".ui.start.StartActivity"
            android:label="@string/title_activity_start" >
        </activity>
        <activity
            android:name=".ui.image.ImageBrowseActivity"
            android:label="@string/title_activity_image_browse" >
        </activity>
    </application>

</manifest>
